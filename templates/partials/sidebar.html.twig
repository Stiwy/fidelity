<div class="bg-gray-800 h-screen overflow-scroll w-2/12 relative">
    <h1 class="text-center text-blue-500 text-2xl mt-10 uppercase tracking-wider font-bold">Robé Médical</h1>
    <p class="text-center text-gray-200 font-italic tracking-wider mt-5 text-md">Programme fidélité</p>

    <nav class="mt-10 text-white w-full mx-auto">
        <ul>
            <li>
                <a id="link_customers" href="{{ path('customer_index') }}" class="text-lg my-3 py-2 px-5 rounded-md w-full block hover:bg-gray-700"><i class="fas fa-users mr-5"></i> Clients</a>
            </li>
            <li>
                <a id="link_offers" href="{{ path('offer_index') }}" class="text-lg my-3 py-2 px-5 rounded-md w-full block hover:bg-gray-700"><i class="fas fa-gift mr-5"></i> Offres</a>
            </li>
            {% if "ROLE_ADMIN" in app.user.roles|json_encode() %}
            <li>
                <a id="link_offers" href="{{ path('user_index') }}" class="text-lg my-3 py-2 px-5 rounded-md w-full block hover:bg-gray-700"><i class="fas fa-user-shield mr-5"></i></i> Administrateurs</a>
            </li>
            {% endif %}
        </ul>
    </nav>

    <div class="absolute bottom-3 text-gray-300 w-full text-center">
        <a class="hover:text-gray-200" href="{{ path('app_logout') }}">Déconnexion</a>
    </div>
</div>